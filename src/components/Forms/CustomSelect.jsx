import React from "react";

const CustomSelect = ({ initialText }) => {
  return (
    <select name="" id="">
      <option value="">{initialText}</option>
    </select>
  );
};

export default CustomSelect;
